version: '3.8'

services:
  web:
    build: .
    container_name: markitdown_web
    ports:
      - "8000:8000"
    volumes:
      - ./static/conversions:/app/static/conversions
    restart: unless-stopped
